// utility function to convert FEN string to object
export function fenToObj(fen) {
    // code to convert fen string to object
  }
  
  // utility function to convert object to FEN string
  export function objToFen(obj) {
    // code to convert object to fen string
  }
  
  // utility function to get the square name
  export function getSquareName(file, rank) {
    // code to get the square name
  }
  
  // utility function to get the square coordinates
  export function getSquareCoords(square) {
    // code to get the square coordinates
  }
  
  